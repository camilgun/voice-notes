{
  "name": "voice-notes",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "transcribe": "./src/transcribe.ts"
  },
  "scripts": {
    "transcribe": "bun run src/transcribe.ts",
    "batch": "bun run src/batch.ts",
    "server:start": "~/whisper.cpp/build/bin/whisper-server -m ~/whisper.cpp/models/ggml-large-v3.bin --convert --port 8080 -l auto -t 4 -p 4",
    "server:stop": "pkill -f whisper-server",
    "build": "bun build src/transcribe.ts --target=bun --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
